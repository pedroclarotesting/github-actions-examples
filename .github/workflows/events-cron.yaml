name: Weekly Cron Job

on:
    schedule:
        - cron: "0 23 * * 0" # 11 pm on sunday in Portugal

jobs:
    run-cron-job:
        runs-on: ubuntu-latest

        steps:
        - name: Testing task
          run: echo "Cron Job tested"
